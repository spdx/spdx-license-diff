{
  "name": "spdx-license-diff",
  "private": true,
  "version": "0.2.0",
  "description": "Diff selected text against SPDX licenses",
  "scripts": {
    "dev": "webextension-toolbox dev",
    "build": "webextension-toolbox build",
    "lint": "eslint app",
    "changelog": "gren c --generate --override -B -t all",
    "attribution": "generate-attribution --production",
    "test": "yarn run lint && yarn install && yarn run build chrome && yarn run build firefox && yarn run build opera && yarn run build edge && yarn run build firefox -m",
    "production": "yarn run lint && yarn install && yarn run build chrome && yarn run build firefox && yarn run build opera && yarn run build edge && yarn run build firefox",
    "buildall": "yarn run production && yarn run changelog && yarn run attribution"
  },
  "standard": {
    "globals": [
      "chrome"
    ]
  },
  "devDependencies": {
    "eslint": "^6.0.1",
    "eslint-config-standard": "^12.0.0",
    "eslint-plugin-import": "^2.18.0",
    "eslint-plugin-node": "^9.1.0",
    "eslint-plugin-promise": "^4.2.1",
    "eslint-plugin-standard": "^4.0.0",
    "eslint-standard": "^1.0.2",
    "github-release-notes": "^0.17.0",
    "oss-attribution-generator": "^1.7.1",
    "standard": "^12.0.1",
    "webextension-toolbox": "latest"
  },
  "dependencies": {
    "diff-match-patch": "^1.0.0",
    "fast-levenshtein": "^2.0.6",
    "jquery": "^3.2.1",
    "md5-jkmyers": "^0.0.1",
    "underscore": "^1.9.1"
  }
}
